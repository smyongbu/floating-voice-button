# 语音识别项目规则

## Windows 与 Android 共用模型

- 本项目所有开发机模型的唯一规范源是 `O:\程序\共享模型仓库`。处理模型前先完整读取该目录的 `README.md` 和 `模型清单.json`；具体位置以工作区根规则为准。
- 新模型先按字节数和 SHA-256 在规范源中查重。完全相同的文件只复用一份；同名但格式、版本或哈希不同的模型分别保存。新增或升级模型时同步维护中央清单、对应平台资源清单、第三方许可、README 与校验测试。
- Windows 端应通过单一解析入口优先使用规范源；需要复制到 `%LOCALAPPDATA%` 时，该副本只是可重建缓存。Windows 的下载、删除与修复逻辑不得把规范源当作可删除的应用私有目录，也不能只靠进程内锁保护多程序共用写入。
- Android App 不能直接读取 O: 盘或 UNC，原生推理也需要手机上的真实本地路径。开发版应由 `android-app\scripts\同步安卓模型.py` 从规范源按清单和哈希增量同步到 `no_backup\resource-packs`；正式版仍下载到同一手机私有结构。手机副本不可避免，但不应在开发电脑上再次联网取得同一文件。
- App 内“删除模型”、卸载、清缓存或同步失败只能影响 Windows／手机的消费副本，不得操作 `共享模型仓库`。规范源文件的删除、覆盖、降级或目录重排属于跨平台破坏性操作，必须先检查所有引用并取得用户明确许可。
- `models` 与 `android-app\local-resources\models` 是现有旧来源目录，不再新增下载。后续迁移必须按“复制到规范源 → 大小与 SHA-256 校验 → 更新解析与测试 → 验证两端 → 经用户许可后再删除旧副本”的顺序进行。
- 共享模型仓库位于项目之外。需要提交的代码使用相对于共同上级目录的路径或 `VOICE_INPUT_MODEL_REPOSITORY` 配置覆盖，不提交模型、本机绝对路径、手机数据、下载临时文件或日志。
